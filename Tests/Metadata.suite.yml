actor: MetadataTester
modules:
    enabled:
        # we use Acceptance module also for this suite
        - \Helper\Acceptance
        # for running shell commands
        - Cli
        # for special asserts when checking xml response
        - Asserts
        # for doing http requests
        - PhpBrowser:
            url: %BASE_URL%